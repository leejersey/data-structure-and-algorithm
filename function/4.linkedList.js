function LinkedList() {

  function Node(data){
    this.data = data;
    this.next = null;
  }

  this.head = null;
  this.length = 0;

  // 末尾添加元素
  LinkedList.prototype.append = function() {

  }

  // 插入元素
  LinkedList.prototype.insert = function(position, data) {
    if (position < ) {

    }
  }
}